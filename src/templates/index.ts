import ts from './ts'
import * as test from './test'
import * as lint from './lint'
import * as ignore from './ignore'
import * as bundler from './bundler'

export default { ts, test, bundler, lint, ignore }
